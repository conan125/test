import { DomDirective } from './dom.directive';

describe('DomDirective', () => {
  it('should create an instance', () => {
    const directive = new DomDirective();
    expect(directive).toBeTruthy();
  });
});
