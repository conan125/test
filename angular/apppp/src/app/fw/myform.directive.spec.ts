import { MyformDirective } from './myform.directive';

describe('MyformDirective', () => {
  it('should create an instance', () => {
    const directive = new MyformDirective();
    expect(directive).toBeTruthy();
  });
});
